#include <tunables/global>

profile unbound flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>

  # Capabilities
  capability net_bind_service,
  capability setgid,
  capability setuid,
  capability sys_chroot,
  capability sys_resource,

  # Network access
  network inet dgram,
  network inet stream,
  network inet6 dgram,
  network inet6 stream,
  network netlink raw,

  # S6 / HA base runtime
  /init rix,
  /run.sh rix,
  /bin/sh rix,
  /bin/bash rix,
  /bin/busybox rix,

  /usr/lib/bashio/** rix,
  /etc/s6/** rix,
  /etc/s6-overlay/** rwix,
  /run/s6/** rwix,
  /run/{s6,s6-rc*,service}/** rwix,
  /package/** rix,
  /command/** rix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,

  /dev/tty rw,
  /dev/null rw,
  /dev/random r,
  /dev/urandom r,

  /lib/** rm,
  /usr/lib/** rm,

  # Required tools used by run.sh and the web UI. Do not replace with a broad usr-bin glob.
  /usr/bin/curl rix,
  /usr/bin/jq rix,
  /usr/bin/grep rix,
  /usr/bin/awk rix,
  /usr/bin/sed rix,
  /usr/bin/sort rix,
  /usr/bin/uniq rix,
  /usr/bin/cat rix,
  /usr/bin/cp rix,
  /usr/bin/mv rix,
  /usr/bin/rm rix,
  /usr/bin/mkdir rix,
  /usr/bin/chmod rix,
  /usr/bin/chown rix,
  /usr/bin/touch rix,
  /usr/bin/find rix,
  /usr/bin/head rix,
  /usr/bin/tail rix,
  /usr/bin/wc rix,
  /usr/bin/tee rix,
  /usr/bin/printf rix,
  /usr/bin/dirname rix,
  /usr/bin/basename rix,
  /usr/bin/xargs rix,
  /usr/bin/test rix,
  /usr/bin/true rix,
  /usr/bin/false rix,
  /usr/bin/sleep rix,
  /usr/bin/seq rix,
  /usr/bin/mktemp rix,
  /usr/bin/pgrep rix,
  /usr/bin/pkill rix,
  /usr/bin/drill rix,

  # Unbound
  /usr/sbin/unbound rix,
  /usr/sbin/unbound-anchor rix,
  /usr/sbin/unbound-checkconf rix,
  /usr/sbin/unbound-control rix,
  /etc/unbound/ rw,
  /etc/unbound/** rw,
  /var/lib/unbound/ rw,
  /var/lib/unbound/** rw,
  /var/run/unbound/ rw,
  /var/run/unbound/** rw,

  # Python / Flask web UI
  /usr/bin/python3* rix,
  /usr/lib/python3*/** rm,
  /web/ r,
  /web/** r,

  # Persistent add-on data
  /data/ rw,
  /data/** rw,

  # Add-on config mount: explicit expert files are read-only.
  /config/ r,
  /config/unbound.conf r,
  /config/unbound-overlay.conf r,
  /config/unbound-extra.conf r,

  # System files
  /etc/passwd r,
  /etc/group r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /etc/nsswitch.conf r,
  /etc/ssl/** r,
  /usr/share/ca-certificates/** r,
  /etc/ca-certificates/** r,

  # Proc and sys
  /proc/sys/net/core/somaxconn r,
  /proc/sys/kernel/random/uuid r,
  owner /proc/*/stat r,
  owner /proc/*/status r,
  owner /proc/*/fd/ r,
  owner /proc/*/fd/** r,

  # Temp: bashio cache + blocklist/root-hints temp files and logs.
  /tmp/ rw,
  /tmp/.bashio/ rw,
  /tmp/.bashio/** rw,
  /tmp/unbound-* rw,
  /tmp/*.tmp rw,
  /tmp/*.log rw,

  # Explicit denies for sensitive HA mounts
  deny /ssl/** rwklx,
  deny /share/** rwklx,
  deny /media/** rwklx,
  deny /backup/** rwklx,

  deny /proc/** w,
  deny /sys/** w,
}