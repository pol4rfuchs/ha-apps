name: 04 Release From Config Deprecated

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Disabled after Release Please migration
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "This workflow is intentionally disabled."
          echo "Release creation is handled by 05-release-please.yml."
          echo "GHCR publishing is handled by 03-build-publish-ghcr.yml on Release Please tags."
