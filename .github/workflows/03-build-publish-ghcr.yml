name: 03 Build Publish GHCR

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/03-build-publish-ghcr.yml"
      - ".github/workflows/build-app.yaml"
      - "*/Dockerfile"
      - "*/config.yaml"
      - "*/rootfs/**"
      - "*/scripts/**"
      - "*/apps/**"
      - "*/package*.json"
  workflow_dispatch:
    inputs:
      app:
        description: "Specific add-on folder to publish, or all"
        required: false
        default: "all"

permissions:
  contents: read

concurrency:
  group: build-publish-ghcr-${{ github.ref }}-${{ github.event.inputs.app || 'all' }}
  cancel-in-progress: false

jobs:
  detect:
    name: Detect add-ons
    runs-on: ubuntu-latest
    outputs:
      apps: ${{ steps.detect.outputs.apps }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Build publish matrix
        id: detect
        shell: bash
        env:
          DISPATCH_APP: ${{ inputs.app || 'all' }}
        run: |
          set -euo pipefail
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          import os
          from pathlib import Path

          expected = [
              "intiface_central",
              "matrix_synapse",
              "navidrome",
              "nginx_proxy_manager",
              "ntfy",
              "ntfy_manager",
              "searxng",
              "teamspeak6",
              "ts6_manager",
          ]
          wanted = os.environ.get("DISPATCH_APP", "all")
          rows = []
          for app in expected:
              if wanted not in ("", "all") and app != wanted:
                  continue
              if Path(app, "Dockerfile").is_file() and Path(app, "config.yaml").is_file():
                  rows.append({"app": app})
          print("apps=" + json.dumps(rows))
          PY

  publish:
    name: Publish ${{ matrix.app }}
    needs: detect
    if: needs.detect.outputs.apps != '[]'
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(needs.detect.outputs.apps) }}
    uses: ./.github/workflows/build-app.yaml
    permissions:
      contents: read
      packages: write
      id-token: write
    with:
      app: ${{ matrix.app }}
      publish: true
