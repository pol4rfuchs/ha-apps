#!/bin/sh
EXIT_CODE="$1"
SIGNAL="$2"
if [ "${EXIT_CODE}" = "256" ]; then
    echo "[livekit] SFU beendet durch Signal ${SIGNAL} (normal bei Container-Stop/-Neustart)"
elif [ "${EXIT_CODE}" = "0" ]; then
    echo "[livekit] SFU sauber beendet (exit code: 0)"
else
    echo "[livekit] SFU unerwartet beendet (exit code: ${EXIT_CODE})"
fi
